# Real-time DoS/DDoS attack detection dashboard built with Flask and Scapy. 
Captures live network traffic, classifies attack types (SYN/UDP/ICMP flood), 
and visualizes alerts with severity scoring.
